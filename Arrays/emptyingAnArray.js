let numbers = [1, 2, 3, 4, 5];
let another = numbers;

// numbers = [];

// numbers.length = 0;
// In this method both the reference variable and main 
//variable gets truncated to length zero.

// numbers.splice(0, numbers.length)

while(numbers.length > 0)
    numbers.pop();