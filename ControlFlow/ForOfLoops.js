const colors = ['black', 'blue', 'pink']
for (let color of colors)
    console.log(color);

// for in is used to iterate over the properties of an object where as.
// for of is used to iterate over the elements in an array.